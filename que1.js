// Input the distance in Kilometer and Convert into Meter and Centimeter.

let distance=10;
let distanceInMeter = distance*1000;
let distanceIncentimeter = distanceInMeter*100;

console.log("distance in km is" + distance);

console.log("distance in meter is" + distanceInMeter);

console.log("distance in cm is" + distanceIncentimeter);